apiVersion: v1
kind: Service
metadata:
  name: {{ include "knievel.fullname" . }}
  labels:
    {{- include "knievel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "knievel.selectorLabels" . | nindent 4 }}
